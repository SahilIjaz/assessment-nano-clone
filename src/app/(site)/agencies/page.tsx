import type { Metadata } from "next";
import Page from "@/generated/pages/agencies";

export const metadata: Metadata = {"title":"Naano for agencies — Brand and creator operations","description":"Choose the Naano workspace built for your agency: manage brand campaigns and budgets, or operate your creator roster."};

export default Page;

import type { Metadata } from "next";
import Page from "@/generated/pages/blog";

export const metadata: Metadata = {"title":"Blog | Naano","description":"Insights on creator-led growth, LinkedIn creators, sponsored-post pricing, founder-led distribution, and the LinkedIn algorithm."};

export default Page;

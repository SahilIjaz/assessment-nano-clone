import type { Metadata } from "next";
import Page from "@/generated/pages/book";

export const metadata: Metadata = {"title":"Book a campaign call — Naano","description":"In 30 minutes, we'll map the right creator angles, campaign format and budget for your ICP. Book a free campaign strategy call with the Naano team."};

export default Page;

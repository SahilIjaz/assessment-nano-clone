import type { Metadata } from "next";
import Page from "@/generated/pages/index";

export const metadata: Metadata = {"title":"Naano: B2B LinkedIn Creator Marketplace","description":"Naano helps B2B SaaS brands run fixed-price LinkedIn creator campaigns and trace attributed clicks and leads back to each post."};

export default Page;

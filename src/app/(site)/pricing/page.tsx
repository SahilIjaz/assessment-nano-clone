import type { Metadata } from "next";
import Page from "@/generated/pages/pricing";

export const metadata: Metadata = {"title":"Pricing | Naano","description":"Naano pricing: Self-Serve is €0/month and each creator sets a fixed offer price. Managed Campaigns is €700/month, done for you."};

export default Page;
